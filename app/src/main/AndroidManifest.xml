<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sliding.mainview">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- SDCard中创建与删除文件权限 -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/app"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".Activity.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.OrderAcitivity" />
        <activity
            android:name=".Activity.ItemSelectActivity"
            android:theme="@style/Base.Theme.AppCompat.Light.Dialog" />
        <activity android:name=".Activity.PayActivity" />
        <activity android:name=".Activity.PayResultActivity" />
        <activity
            android:name=".Activity.InsertNewTable"
            android:theme="@style/Base.Theme.AppCompat.Light.Dialog" />
        <activity
            android:name=".Activity.InsertNewFood"
            android:theme="@style/Base.Theme.AppCompat.Light.Dialog" />
        <activity android:name=".Activity.PersonalSetting" />
        <activity android:name=".Activity.MainActivity" />
        <activity android:name=".Activity.UserActivity" />
        <activity android:name=".Activity.StartActivity" />
        <activity android:name=".Activity.AboutMe"></activity>
    </application>

</manifest>
